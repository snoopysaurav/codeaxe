import Comparision from "@/screen/Comparision";
import Faqs from "@/screen/Faqs";

const Homepage = () => {
  return (
    <main>
      <section>
        <Comparision />
      </section>
      <section>
        <Faqs />
      </section>
    </main>
  );
};

export default Homepage;
